services:
  - type: web
    name: brokerflow
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn broker_flow:app --host 0.0.0.0 --port $PORT"
    plan: free
    envVars:
      - key: CLICKUP_TOKEN
        sync: false
